#include<stdio.h>

    int main(){
        int radius =8l, area;
        float pie =3.14587;
        area = pie*(radius)*(radius);
        printf("the radius entered is %d and area is %f " , radius , area );
    return 0;
}